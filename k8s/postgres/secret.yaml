# DO NOT commit real values to Git
# Create via kubectl instead:
#
# kubectl create secret generic postgres-secret \
#   --from-literal=postgres-password=mlflow123 \
#   -n mlops

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: mlops
type: Opaque
stringData:
  postgres-password: "mlflow123"